# 분리수거 검사관 (Recycle Inspector) 구현 플랜

작성: 2026-08-30. 설계 근거: llm-wiki `wiki/topics/분리수거 검사관 게임 설계.md`

## 방향 요약

Papers, Please 스타일의 판정 게임. 주민이 봉투를 들고 오면 오늘의 배출일 규칙으로 [통과]/[반려] 도장을 찍는다.
학생이 먼저 검사관을 해 보고, 이후 신입 AI "재활용이"에게 근무를 맡기고 감독하며 **알바 수첩**을 채워 가르친다.
AI는 수첩에 없는 물건을 주사위로 찍고, 학생은 [잠깐!]으로 즉시 교정한다(교정 = 수첩 기록 = 학습).

품질 원칙: **게임 자체가 재밌고 세련되어야 한다.** 밝은 톤 + 원작의 구도(창구·책상·실물 UI)와 손맛(도장 쾅).

## 기술 스택

- 정적 HTML/CSS/JS (빌드 도구 없음) — 패드 브라우저에서 URL만으로 실행, GitHub Pages 배포 가능
- 그래픽: 인라인 SVG (일관된 팔레트의 플랫 일러스트) + CSS 애니메이션. 외부 에셋 의존 없음
- 사운드: WebAudio 합성 (도장 쾅, 삑, 성공음) — 외부 파일 없음, 학교망 안전
- 저장: localStorage (수첩, 기록). 서버·계정 없음

## 단계별 플랜

### 1단계 — 검사관 코어 (사람 플레이) ✅ 완료 (2026-08-30)
- 검사대 화면: 창구(주민+말풍선), 책상(봉투·물건 카드), 벽 공지판(오늘의 배출일), 도장 2개
- 주민 등장 → 한 줄 대사 → 봉투 열기 → 물건 카드 펼침 → 물건 탭 확대 확인
- [통과]/[반려] 도장: 슬램 애니메이션 + 잉크 자국 + 타격음. 반려 시 사유 자동 표기
- 판정 채점: 봉투 안에 오늘 품목 외 물건이 하나라도 있으면 반려가 정답
- 즉시 피드백: 오판 시 벌점 통지서 슬라이드 인
- 하루(5봉투) 정산 화면: 정확도, 통지서 목록, 다시 하기
- 물건 12종(종이 5, 캔 2, 플라스틱 3, 음식물 2) + 주민 5명 SVG

완료 기준: 패드 크기 화면에서 도장 손맛이 나고, 1일차 근무(종이류 날)를 처음부터 정산까지 플레이 가능.

### 2단계 — AI 재활용이 (감독 플레이) ✅ 완료 (2026-08-30)
- 알바 수첩 UI: 4칸 열람 오버레이(재활용 안 됨 칸은 3단계에서 개방). 기입은 교정 채널로 일원화
- AI 근무 모드: 재활용이가 봉투를 순회하며 판정 근거를 말풍선으로 출력, 수첩에 없으면 🎲 찍기
- [잠깐!] 개입: 유예 타이머 내 탭 → 물건 분류 교정 → 수첩 자동 기록 → 판정 재계산
- 놓친 오류 → 정산 통지서. 정확도 전/후 비교 표시(내 기록 vs 재활용이 기록)

### 3단계 — 차시 콘텐츠 🟡 핵심 완료 (2026-08-31, 양념 2종 남음)
- 날짜 진행: 1일차(종이) → 2일차(캔) → 3일차(혼합). 배출일 규칙 변경에도 수첩 지식 유지 확인
- 2차시 도구: 공통점 메모(특징 태그 조합) — 물건 풀 30종+로 확대, 새 물건 일반화
- 3차시 도구: 주의 포스트잇(예외 규칙) + 킬러 아이템(영수증, 기름 피자박스, 스프링 공책) + 🎲 숨김 챌린지
- 떼쓰는 주민 이벤트(낮은 빈도)

### 4단계 — 폴리시·수업 지원
- 결과 요약 화면(활동지 대응: 오늘 가르친 것/정확도 변화) — 교사 확인용
- 사운드·연출 다듬기, 캐릭터 표정 2종, 패드 실기기 최적화(터치 타깃 크기, 세로/가로)
- GitHub Pages 배포, 학교망 접속 테스트

## 일정 (llm-wiki 타임라인과 동기)

- ~09-06: 1단계 (중간평가 주간과 병행, 무리하지 않음)
- 09-08~14: 2단계 + 3단계 착수
- 09-11: 이 설계 기준 3차시 지도안 (별도 문서)
- 09-15~19: 4단계 + 실기기 테스트
- 09 4주: 수업 적용

## 데이터 모델 (1단계)

```js
ITEM = { id, name, cat: "paper"|"can"|"food"|"plastic"|"trash", svg }
BAG  = { person, line, items: [ITEM] }        // 주민, 대사, 내용물
DAY  = { rule: "paper", bags: [BAG] }          // 오늘의 배출일
판정  = 통과 ⇔ bag.items 전원 cat === rule
```

## 구현 노트 (2026-08-30, 2단계)

- 주사위 찍기는 순수 무작위가 아니라 **오늘 배출일 쪽으로 60% 편향**. 순수 무작위는 "반려"를 남발해 훈련 전에도 정답률이 높아지는 문제가 있었음. 편향 덕에 "잘못 통과" 실수가 주로 발생하고, 무훈련 기준 정확도 ~40%대로 훈련 동기가 생김.
- [잠깐!] 유예는 4.5초. 놓친 실수는 정산 통지서("재활용이의 실수 — …")로 회수.
- 교정 = 분류 선택 → 수첩 자동 기록(localStorage) → 재판정. 도장이 아니라 분류를 고치는 원칙 유지.
- AI 근무 봉투는 매 회차 무작위 생성(오염 55%), 사람 근무(1일차)는 고정 시나리오.
- 재활용이 목소리는 사각파 전자음 블립, 배움 순간은 상승 3음 차임, 주사위는 노이즈 래틀.

## 구현 노트 (2026-08-31, 학습 경험 개편)

유하님 피드백 "학습시키는 느낌이 적다" 반영 + 에이전트다움 보강.

- **출근 전 오리엔테이션**: 수첩이 빈 상태에서 AI 근무를 시작하면 재활용이가 "딱 3개만 가르쳐 주세요"라고 요청. 전체 물건 중 3개를 골라 가르침(우선순위 판단 발생). 전임 알바 시드 제거 — 처음부터 내가 키운 AI.
- **방침(policy) 가르치기**: "모르는 물건이 나오면?" → 일단 통과 / 일단 반려 / 찍어볼게. 사실(지식)과 별개로 행동 방침을 학생이 정함 — 불확실성 아래 위험 성향이라는 에이전트 고유 개념. 오리엔테이션에서 선택, 수첩에서 언제든 변경. 미지 물건 처리 로직이 방침을 따름(통과=오늘 규칙으로 간주 / 반려=reject / 찍기=기존 60% 편향).
- **등장 시에만 교정**: 수첩은 열람 전용으로 회귀(방침만 편집 가능). 사실 가르치기 채널 = 오리엔테이션(3개) + [잠깐!] 교정 + **정산 통지서의 "○○ 가르치기" 버튼**(실수와 관련된 미학습 물건만).
- **가르친 보람 연출**: 가르친 물건을 다음에 만나면 "아까 가르쳐 주신 거다! ✨" + 배움 차임. 정산에 "오늘 새로 배운 것 +N개" 표시.
- 검증: 신규 상태 → 오리엔테이션 3개+방침(일단 반려) → 근무에서 미지 물건 "몰라서 반려" → 깨끗한 봉투 2건 억울한 반려 통지서(방침 트레이드오프 증명) → 정산 60%, +3개 표시, 통지서에서 공책 가르치기 → 수첩 기록·중복 버튼 done 처리 확인.

## 구현 노트 (2026-08-31, 3단계 핵심)

- **날짜 시스템**: 시작 화면 칩으로 1일차(종이·기본 12종) / 2일차(캔·+8종=20종) / 3일차(플라스틱·어려움·+킬러 4종=24종) 선택. 선택은 저장되어 교사가 차시별로 지정. 1일차 사람 근무만 고정 시나리오, 나머지는 풀 기반 무작위.
- **물건 24종 + 특징(feat) 태그**: flat/printed/shiny/container/light/foodish/dirty. 신규: 전단지·달력·햄통조림·세제통·빨대·장난감블록·수박껍질·귤껍질 + 킬러(영수증·기름 피자박스·국물 남은 컵라면·달걀 껍데기).
- **공통점 규칙 = 추측-승인**: 수첩에 같은 특징 조합의 물건이 2개 이상 같은 분류로 쌓이고 반례가 없으면, AI 근무 정산 후 재활용이가 가설을 제안("납작하고 글자가 있는 것은 다 종이류 아니에요?"). 승인 → 규칙 저장, 거절 → 재제안 안 함. 후보 4종(종이/캔/플라스틱/음식물) 사전 정의.
- **추론 우선순위**: 수첩 정확 지식 > 공통점 규칙(📐 보라 태그) > 방침. 규칙 추론 시 근거를 말로 설명.
- **재활용 안 됨(trash) 분류**: 5번째 칸·5번째 가르치기 버튼. 어떤 배출일에도 통과 불가.
- **일반화의 배신**: 영수증(flat+printed)이 종이 규칙에 걸려 잘못 통과 → 통지서 → ❌로 가르치면 정확 지식이 규칙보다 우선 + 수첩 규칙 카드에 "⚠ 하지만 영수증은 재활용 안 됨!" 예외 자동 표시. 규칙 삭제(×)도 가능.
- 3일차 봉투에 킬러 아이템 최소 2회 등장 보장. 방침 찍기에서 trash는 제외(GUESS_CATS).
- 남은 양념: 떼쓰는 주민 이벤트, 🎲 숨김 챌린지 → 4단계와 함께.
- 검증: 가설 엔진 단위 검증(제안 조건·반례 차단·재제안 방지), 규칙 매칭(전단지 O/상자 X/영수증 오작동 O), 예외 표시, 킬러 봉투 보장, 가설 오버레이·수첩 규칙 카드 시각 확인.

## 구현 노트 (2026-08-31, 4단계 — 오늘의 훈련 카드)

- **정산 화면 [📋 오늘의 훈련 카드 보기] 버튼** → 한 장짜리 카드 오버레이. 활동지에 그대로 옮겨 적는 용도(연구 데이터 수집 지점).
- 카드 내용: 일차·모드·회차 / 검사 정확도(✅·❌ 포함) / 📈 재활용이 성장(회차별 % 화살표 연결, 최신 강조) / 📒 오늘 가르친 것(물건→분류 칩, 오리엔테이션·[잠깐!]·통지서 가르치기 전부 포함) / 📐 우리가 만든 규칙(예외 "⚠ ○○ 빼고!" 표기) / 🧭 방침 / 수첩 현황(N/24).
- 구현: `S.taughtThisShift` Set으로 근무 단위 가르침 추적. 근무 진입 버튼 4곳에서 리셋하되 오리엔테이션(진입 후·startDay 전)은 보존. 카드는 열 때 렌더 — 통지서에서 가르친 것도 바로 반영.
- 검증: DOM 스텁 하네스로 렌더 로직 15항목 통과(AI 2회차 시나리오: 정확도·회차·가르친 3개·유령 id 필터·규칙 예외·방침·성장 이력 / 인간 모드·빈 기록 시나리오: 없음 문구 3종). node --check 통과.

# ═══════════ 2차 개편(v2) 구현 플랜 — 2026-08-31 ═══════════

설계 스펙: llm-wiki `vaults/앱-개발/topics/` — `분리수거 검사관 2차 개편 구상.md`, `분리수거 검사관 아이템 조합표.md`

**개발 방식**: v1은 루트에 그대로 유지(수업 백업·기존 링크 보존), v2는 **`/v2/` 폴더**에서 개발.
push하면 `uhakim.github.io/recycle-inspector/v2/`에서 폰 테스트 가능. 완성 후 루트 교체(v1은 `/v1/`로 이동).

- **0단계 — 데이터 & 시뮬 검증**: 특징 21·물건 100 데이터 모듈(`v2/data.js`), 조합표 밸런스 자동 검증 스크립트(규칙 정답률 궤적·미끼 불변식·애매이 보장·개수 검증)
- **1단계 — 판정 엔진 v2**: 물품 지식 > 특징 규칙(구체성 우선, 동점=혼란→방침) > 방침. 규칙 슬롯 8, 물품 등록 3개/회, 규칙별 적중/실점 통계. 헤드리스 단위 테스트
- **2단계 — 수첩 두 탭 UI**: [물건] 탭(등록 목록+특징 아이콘) / [특징] 탭(감각기관별 도감 21 + 특징 집계 + 규칙 조립·슬롯 8) + 방침. 가르치기 플로우(분류 → "뭘 보고 알았어?" 특징 1~2개 탭)
- **3단계 — 도전 모드 & 튜토리얼**: 출제 3단계(대본 100점 / 반대본 50~60% / 랜덤), 무개입 도전 연출(기존 창구·도장 재사용), 정산(규칙별 성적, 물건/규칙 분리 집계, 오답 가르치기 3개 제한), 재활용이 대사 전환 이벤트(특징 탭 개방)
- **4단계 — 튜닝 히스토리 & 성찰**: 시도별 스냅샷·변경점 diff, 점수 곡선(최고 ⭐), 성찰 화면, 구글폼 prefill 제출(폼 생성은 유하님 계정)
- **5단계 — 아트 & 폴리시**: SVG 100종(특징 조합 절차 생성 + 품목별 악센트), 대사·사운드, 패드 최적화
- **6단계 — 통합 검증 & 배포**: 시뮬 풀플레이, 실기기 테스트, 루트 전환

일정(안): 8/31 0~1단계 → 9/1~2 2단계 → 9/3~6 중간평가 우선(개발 최소) → 9/7~10 3~5단계 → 9/11 지도안(v2 화면) → 9/15주 실기기.

## v2 구현 노트 (2026-08-31~09-01, 0~2단계)

- 0단계: v2/data.js(특징 21·물건 100·설계 규칙 지도·상수), tools/sim-v2.mjs 밸런스 검증 ALL PASS.
- 1단계: v2/engine.js — 등록>규칙(구체성, 동점=혼란→방침+충돌기록)>방침, 슬롯 8, 도전2 전용 봉투 빌더, 규칙별 성적. tools/test-engine-v2.mjs 17항목 ALL PASS(도전1 100점 보장, 도전2 64%, 튜닝 상승 84→86%, 과잉 개별화 하락 86→80%).
- 2단계: v2/index.html·style.css·game.js — 알바 수첩 두 탭([물건]: 분류 칸+학생이 고른 특징 아이콘+? 미스터리 / [특징]: 감각기관 도감·관찰 노트 집계(여러 분류 걸침 ⚠ 경고)·규칙 조립기·슬롯 게이지·규칙 성적), 방침, 가르치기 2단 모달(분류→"뭘 보고 알았어?" 특징 1~2개), 간이 도전·정산(오답 가르치기 3개/회 예산), 시도 기록, localStorage 3키, 리셋. 브라우저 자동 조작으로 루프 전체 검증(예산 차단 알림·garbage-in·방침 트레이드오프 재현). 엔진 확인용 놀이터는 v2/lab.html로 이동.
- 남은 것: 3단계(창구 연출·튜토리얼 대사·출제 대본 진행), 4단계(히스토리 diff·성찰·구글폼), 5단계(SVG 100종), 6단계(루트 전환).
